package com.yjgs.bll.ibll;

import java.util.ArrayList;

import com.yjgs.dcl.NewsList;
import com.yjgs.dcl.ProductList;

public interface ISearchBll {

	/**
	 * 产品搜索
	 * 
	 * @param fKeyWord		搜索关键字
	 * @return						返回产品列表信息集合
	 */
	public ArrayList<ProductList> productSearch(String fKeyWord);
	
	/**
	 * 新闻搜索
	 * 
	 * @param fKeyWord		搜索关键字
	 * @return						返回新闻列表信息集合
	 */
	public ArrayList<NewsList> newsSearch(String fKeyWord);
}
